#include "../../Flutter/Flutter-Debug.xcconfig"
#include "Warnings.xcconfig"

// Ad-hoc signing gives this local build a stable macOS app identity without
// requiring an Apple Developer certificate. It is needed for notifications.
CODE_SIGNING_ALLOWED=YES
CODE_SIGN_IDENTITY=-
COPYFILE_DISABLE=1
