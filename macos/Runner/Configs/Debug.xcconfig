#include "../../Flutter/Flutter-Debug.xcconfig"
#include "Warnings.xcconfig"

// Swift Package Manager currently copies Finder metadata into Firebase
// artifacts on this machine. Keep local debug builds unsigned until those
// generated artifacts can be signed reliably.
CODE_SIGNING_ALLOWED=NO
COPYFILE_DISABLE=1
