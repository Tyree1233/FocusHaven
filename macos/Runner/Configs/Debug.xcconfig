#include "../../Flutter/Flutter-Debug.xcconfig"
#include "Warnings.xcconfig"

// Local development builds do not need an Apple signing identity. Skipping
// signing avoids Finder metadata errors when running the app from this Mac.
CODE_SIGNING_ALLOWED=NO
